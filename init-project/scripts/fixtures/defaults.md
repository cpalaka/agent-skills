---
fork: git-flow-squash
knobs:
  tracker-github:
    REPO: "<owner/repo>"
    RESULTS_DIR: "none"
---

Fixture engine defaults: the fork and the tracker's key shape. The fixture Profiles' own blocks
are all Profile-only, so they follow the tracker block in the Profile's order.
