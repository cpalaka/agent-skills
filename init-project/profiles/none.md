---
imports: []
templates: []
---

The explicit type for a project no Profile fits (a Python library, a C++ tool): it adds no import,
no Template, no fragment and no settings, and takes every engine default in `defaults.md`. Every
shape-valued default is answered in the answers file, as `defaults.md` glosses it.
